# Codex Signature Set Backlog

## Now

- Keep the collection-first verification signal green in the main workspace, not just in a clean temp install.
- Tighten the collection shelf and set-detail composition so the line reads like a shipped mini collection, not a feature stack.
- Keep polishing the premium box/studio/instruction surfaces with SKU-aware accents and stronger product photography cues.
- Audit `App.tsx` for any remaining single-flagship assumptions after the collection pivot.

## Next

- Add committed browser-level smoke coverage for collection landing -> set detail -> studio -> instructions -> keepsakes -> commission flow.
- Add more structural tests for multi-SKU determinism, manifest totals, and export determinism.
- Add collection-specific prompt/spec docs for shelf, box system, studio, trailer, and commission flow.
- Make the keepsake/export layer feel more product-grade across all four launch SKUs.

## Later

- Upgrade the `.io` bundle path with richer Studio-oriented metadata.
- Add a more elegant support strategy for standing versions of the collectible archetypes.
- Add film/sound synchronization that derives directly from instruction phases and collection trailer beats.
- Expand the commission flow once the four-SKU launch line feels unquestionably coherent.

## Blocked

- No current code blocker. The main remaining risk is mixed worktree hygiene until the collection-first pass is split into a clean checkpoint.

## Done

- Locked the product around the monochrome Codex collectible instead of a generic blockified demo.
- Made `ModelIR` the canonical model layer and `ScenePack` a derived projection.
- Added signature-set assemblies, plinth support, and real export bundle generation.
- Added deterministic tests for the canonical engine.
- Added the first live 3D builder studio slice.
- Added the first instruction-book artifact generator.
- Added prompt/spec docs and a permanent devlog/backlog system for the flagship track.
- Replaced the old hidden-canvas reveal behavior with phase-driven studio playback.
- Reworked the set-box hero surface toward a more premium product-photography composition.
- Upgraded the instruction artifact and aligned the in-app instruction preview to it.
- Improved studio lighting, stage geometry, and materials for a stronger collectible read.
- Added a collection-first launch line with four SKUs: `COD-001`, `BLM-001`, `AIC-001`, and `KC-001`.
- Added collection-layer canonical packaging/instruction/trailer projections.
- Added a collection shelf landing flow and updated onboarding coverage to match the new collection-first experience.
- Re-established clean `npm test`, `npm run lint`, and `npm run build` verification in the active workspace.
- Added a high-signal app smoke test for landing -> box -> studio -> instructions -> keep -> shelf reset.
- Tightened the `ScenePack` manifest role contract so it stays aligned with the real-set export model.
- Added a manual browser smoke pass in the active workspace for landing -> box -> studio -> instructions -> keep.
