# Codex Signature Set Backlog

## Now

- Get a clean compile signal for the new Three.js builder studio path.
- Polish the `Codex Signature Set` set-box surface so it reads as a premium original product.
- Tighten the builder studio around the canonical phases: orbit, phase stepper, explode/assemble, instruction sync.
- Strengthen the instruction-book rendering language with clearer step composition and subassembly handling.

## Next

- Add prompt/spec docs for set box, studio, instructions, film, and sound.
- Add more structural tests for `ModelIR`, manifest totals, and export determinism.
- Add smoke coverage for image input -> set box -> studio -> instructions -> keepsakes.
- Make the keepsake/export layer feel more product-grade.

## Later

- Upgrade the `.io` bundle path with richer Studio-oriented metadata.
- Add a more elegant support strategy for standing versions of the Codex collectible.
- Add film/sound synchronization that derives directly from instruction phases.
- Expand beyond the Codex flagship once the canonical path is unquestionably solid.

## Blocked

- Full `tsc` and `vite build` verification remains flaky in the current environment and needs another pass.
- `npm run lint` has not yet been re-run after the signature-set engine changes.

## Done

- Locked the product around the monochrome Codex collectible instead of a generic blockified demo.
- Made `ModelIR` the canonical model layer and `ScenePack` a derived projection.
- Added signature-set assemblies, plinth support, and real export bundle generation.
- Added deterministic tests for the canonical engine.
- Added the first live 3D builder studio slice.
- Added the first instruction-book artifact generator.
- Added prompt/spec docs and a permanent devlog/backlog system for the flagship track.
- Replaced the old hidden-canvas reveal behavior with phase-driven studio playback.
